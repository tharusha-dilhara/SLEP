import 'package:flutter/material.dart';
import 'package:slep/const/colors.dart';

const support_icon = ImageIcon(
  AssetImage("lib/asset/icons/support.png"),
);

const setting_icon = ImageIcon(
  AssetImage("lib/asset/icons/setting.png"),
);

const home_icon = ImageIcon(
  AssetImage("lib/asset/icons/home.png"),
);

// const timer = ImageIcon(
//   AssetImage("lib/asset/icons/timer.png"),
//   color: slep_blue,
// );

const location = Icon(
  Icons.location_on_outlined,
  size: 15,
  color: shape_filling_dark,
);

// const location = ImageIcon(
//   AssetImage("lib/asset/icons/location.png"),
//   color: slep_blue,
// );

const timer = Icon(
  Icons.schedule,
  size: 15,
  color: shape_filling_dark,
);

const next = ImageIcon(
  AssetImage(
    "lib/asset/icons/next.png",
  ),
  color: shape_filling_dark,
);

const pf = ImageIcon(AssetImage(
  "lib/asset/icons/pf.png",
));
